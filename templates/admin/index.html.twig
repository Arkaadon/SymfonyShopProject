{% extends 'base.html.twig' %}

{% block title %}Bamazon{% endblock %}

{% block body %}
  <style>
    .example-wrapper {
      margin: 1em auto;
      max-width: 800px;
      width: 95%;
      font: 18px/1.5 sans-serif;
    }
    .example-wrapper code {
      background: #F5F5F5;
      padding: 2px 6px;
    }
  </style>

  <div class="d-flex justify-content-center align-items-center flex-column">
    <h1>Gestion Administrateur Bamazon</h1>
    <h3>Vous êtes connecté en tant que
      {{ app.user.username }}</h3>

    <a href="{{ path('app_logout')}}" class="btn btn-primary mt-2">Logout</a>
    <a href="{{ path('app_homepage')}}" class="btn btn-primary mt-2">Accueil</a>
    <a href="{{ path('admin')}}" class="btn btn-primary mt-2">Page d'Administration</a>
  </div>

  <div class="d-flex justify-content-center align-items-center mt-5">
    <a href="{{ path('product_list') }}" class="btn btn-primary mt-2">Voir les Produits</a>
    <a href="{{ path('discount_list') }}" class="btn btn-primary mt-2">Voir les Codes promotionnels</a>
    {% if is_granted('ROLE_SUPER_ADMIN') %}
    <a href="{{ path('user_management')}}" class="btn btn-primary mt-2">Gestion des comptes utilisateurs</a>
    {% endif %}
  </div>
{% endblock %}
